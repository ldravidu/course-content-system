# Development-specific configuration

# Use embedded database for development
# These settings will be overridden by the embedded PostgreSQL setup in code

# Disable certain security features during development
spring.security.user.name=dev
spring.security.user.password=dev

# Enable more verbose logging
logging.level.org.springframework=DEBUG
logging.level.com.coursecontentsystem=TRACE